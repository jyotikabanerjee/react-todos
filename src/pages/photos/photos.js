import React from 'react';
import PropTypes from 'prop-types';

const Photos = ({}) => {
    return (
        <div>
            Build something related to photos here
        </div>
)
    ;
};

Photos.propTypes = {};
Photos.defaultProps = {};

export default Photos;
